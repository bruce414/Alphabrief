# Frontend
# Recommended (Vite proxy forwards /api → backend): leave unset or empty.
#
# Direct-to-backend (no proxy): must include /api/v1 — not just the host:
# VITE_API_BASE_URL=http://localhost:8000/api/v1
#
VITE_API_BASE_URL=

