# Frontend
# If you prefer calling the backend directly (no dev proxy), use:
# VITE_API_BASE_URL=http://localhost:8000

# If you use the built-in dev proxy (recommended), you can keep this empty.
VITE_API_BASE_URL=

